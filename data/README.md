# Data Directory

This repository intentionally ships **no binary imaging files** and **no private patient data**.

Use this directory for your own dataset manifests and local image storage when reproducing experiments.
The training and inference configuration currently points to CSV manifest templates in `data/manifests/`.
